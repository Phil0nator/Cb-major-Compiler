#ifndef __ALLOCATOR
#define __ALLOCATOR

template<typename T>
struct allocator{

    inline T* allocate(size_t n){
        return mapalloc(n*sizeof(T));
    }

    inline void deallocate(T* block){
        return mapfree(block);
    }
    

};




#endif